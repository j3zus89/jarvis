# Real secrets go in %USERPROFILE%\.hermes\.env (or server/.env) — NEVER in
# this repo. This file only documents which variables the AI Router reads.
# See docs/AI_ROUTER.md.

GROQ_API_KEY=
GEMINI_API_KEY=
OPENROUTER_API_KEY=
FISH_AUDIO_API_KEY=   # server/scripts/tts_jarvis.py — standalone TTS test script
